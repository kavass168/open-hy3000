--- a/src/gpio-button-hotplug.c
+++ b/src/gpio-button-hotplug.c
@@ -191,7 +191,7 @@ static void gpio_keys_gpio_report_event(struct gpio_button_data *bdata)
        input_event(input, EV_KEY, bdata->button->code, state);
        input_sync(input);

-       broadcast_uevent(event->skb, 0, 1, GFP_KERNEL);
+       kobject_uevent(&bdata->dev->kobj, KOBJ_CHANGE);
 }
